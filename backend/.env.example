# SQLite xsell app (default — no config needed)
# DB file: backend/xsell.db

# Oracle (CONVERSIONS_FINAL exclusions — required when exclusions enabled)
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_HOST=
ORACLE_PORT=1521
ORACLE_SERVICE=
